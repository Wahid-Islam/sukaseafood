# --- API ---------------------------------------------------------------------
fastapi>=0.116.0
uvicorn[standard]>=0.35.0
pydantic>=2.0
pydantic-settings>=2.0
python-multipart>=0.0.20
httpx>=0.27.0

# --- PostgreSQL --------------------------------------------------------------
sqlalchemy>=2.0.40
asyncpg>=0.30.0          # async driver used by the application
psycopg[binary]>=3.2.0   # sync driver used by Alembic
greenlet>=3.0.0          # required by SQLAlchemy's async bridge
alembic>=1.14.0

# --- Firebase ----------------------------------------------------------------
# firebase-admin is NOT here on purpose. It is optional at runtime: without it,
# app/firebase.py still builds public Storage download URLs, and only signed
# URLs and server-side uploads are unavailable.
#
# It also drags in `cryptography`, which builds from Rust source on any
# interpreter or platform without a matching prebuilt wheel — turning a two
# minute install into a toolchain problem for every developer on the team, in
# exchange for a feature I1 does not use.
#
# Install it explicitly when you need signed URLs or uploads:
#     pip install -r requirements-firebase.txt
